# SPA fallback for dashboard routes — all paths under /dashboard
# serve dashboard.html so the client-side router can handle them.
/dashboard              /dashboard.html  200
/dashboard/*            /dashboard.html  200
