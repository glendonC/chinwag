{
  "name": "chinmeister-worker",
  "version": "0.1.0",
  "private": true,
  "license": "BUSL-1.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --env local",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:coverage": "vitest run",
    "typecheck": "tsc --noEmit",
    "build:pricing-seed": "node --experimental-strip-types scripts/fetch-pricing-seed.ts"
  },
  "dependencies": {
    "@chinmeister/shared": "file:../shared"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.4",
    "@cloudflare/workers-types": "^4.20260402.1",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2",
    "wrangler": "^4.0.0"
  }
}
